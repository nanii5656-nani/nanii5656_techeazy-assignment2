#!/bin/bash
# verify-list.sh
# Usage: ./verify-list.sh <bucket-name>
# This script verifies that the read-only IAM role can list S3 files

BUCKET=$1

if [ -z "$BUCKET" ]; then
  echo "Usage: $0 <bucket-name>"
  exit 1
fi

echo "Listing files in bucket: $BUCKET"
aws s3 ls s3://$BUCKET/
